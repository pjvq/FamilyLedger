# Shared SAN configuration for FamilyLedger TLS certificates.
# Used by both generate-self-signed.sh and rotate-cert.sh.
# Edit this file to change IP addresses or DNS names, then regenerate.

# Server IP address (primary)
SERVER_IP="124.222.52.10"

# Additional SANs
EXTRA_IPS="127.0.0.1"
EXTRA_DNS="localhost familyledger.local"

# Certificate validity (days)
CA_DAYS="7300"       # 20 years
SERVER_DAYS="3650"   # 10 years
