timeout: 5s

tags:
  flaky:
    # Flaky tests run separately, non-blocking
    # Must have linked issue + fix deadline (1 sprint)
  integration:
    timeout: 15s
    # Requires running Go server + PostgreSQL
    # Run with: flutter test test/integration_test/
  e2e:
    timeout: 30s
    # Full end-to-end with real server
    # Run via CI flutter-e2e.yml workflow
